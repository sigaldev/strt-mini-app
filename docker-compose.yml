version: "3.9"

services:
  client:
    build: ./
    container_name: client_frontend
    ports:
      - '5173:5173'
    volumes:
      - ./:/app
      - /app/node_modules
    working_dir: /app
    stdin_open: true
    tty: true
